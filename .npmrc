# CRA (react-scripts 5) declares an outdated peer range of typescript ^3||^4,
# while this project uses TypeScript 5 — which works fine in practice. Skip
# strict peer resolution so a plain `npm install` succeeds.
legacy-peer-deps=true
