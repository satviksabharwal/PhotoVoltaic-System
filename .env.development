# Client (Create React App) environment — DEVELOPMENT
# Loaded automatically by `react-scripts start`.
# The backend runs on port 5500 during local development.
REACT_APP_API_URL=http://localhost:5500/api
